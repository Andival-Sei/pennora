# Дизайн и архитектура категорий

## 1. Дизайн UI/UX

### Структура категорий

Категории поддерживают:

- **Иерархию** (родительские и дочерние категории)
- **Типы**: `income` (доходы) и `expense` (расходы)
- **Визуальное оформление**: иконка + цвет
- **Сортировку**: `sort_order` для кастомного порядка
- **Архивацию**: `is_archived` для скрытия неиспользуемых

### Основные экраны

#### 1. Список категорий (`/dashboard/categories`)

**Вариант A: Древовидная структура (рекомендуется)**

```
📊 Доходы
  ├─ 💼 Зарплата
  ├─ 🎁 Подарки
  └─ 📈 Инвестиции
     └─ 💹 Дивиденды

💸 Расходы
  ├─ 🏠 Жильё
  │  ├─ 🏘️ Аренда
  │  ├─ ⚡ Коммунальные
  │  └─ 🔧 Ремонт
  ├─ 🍔 Еда
  │  ├─ 🛒 Продукты
  │  └─ 🍽️ Рестораны
  └─ 🚗 Транспорт
     ├─ ⛽ Топливо
     └─ 🚇 Общественный транспорт
```

**Вариант B: Группированный список**

- Группы по типам (Доходы / Расходы)
- Внутри группы — плоский список с отступами для подкатегорий
- Возможность сворачивания/разворачивания групп

**Компоненты:**

- `CategoryTree` — древовидное отображение
- `CategoryItem` — элемент категории (иконка, цвет, название)
- `CategoryGroup` — группа по типу (income/expense)
- Кнопка "Добавить категорию" (FAB или в шапке)

#### 2. Форма создания/редактирования категории

**Поля:**

- Название (обязательно)
- Тип: Доход / Расход (радио-кнопки или селект)
- Родительская категория (опционально, селект с поиском)
- Иконка (выбор из набора, например lucide-react icons)
- Цвет (color picker или предустановленные цвета)
- Порядок сортировки (автоматически или вручную)

**Компонент:** `CategoryForm`

#### 3. Выбор категории (в транзакциях)

**Варианты:**

- Модальное окно с поиском и фильтрацией по типу
- Выпадающий список с группировкой
- Быстрый выбор из недавно использованных

**Компонент:** `CategorySelector`

### Визуальный стиль

**Цвета:**

- Доходы: зелёные оттенки (#10b981, #059669)
- Расходы: красные/оранжевые оттенки (#ef4444, #f97316)
- Нейтральные: серые (#6b7280) для общих категорий

**Иконки:**

- Использовать lucide-react (уже в проекте)
- Популярные: Home, ShoppingCart, Car, Utensils, Heart, etc.

**Адаптивность:**

- Мобильная версия: компактный список с возможностью раскрытия
- Десктоп: древовидная структура с drag-and-drop для сортировки

## 2. Хранение данных

### Архитектура: Офлайн-first

**Основная БД: Supabase (PostgreSQL)**

- ✅ Уже есть таблица `categories` в схеме
- ✅ Поддержка иерархии через `parent_id`
- ✅ RLS для безопасности
- ✅ Типы из `lib/db/supabase/types.ts`

**Локальное хранилище: IndexedDB (планируется)**

- Для офлайн-режима
- Синхронизация через `SyncManager`
- Пока не реализовано, но архитектура предусмотрена

### Стратегия хранения

**Текущий этап (MVP):**

1. Хранить только в Supabase
2. Использовать React Query или SWR для кеширования
3. Оптимистичные обновления UI

**Будущее (полная реализация):**

1. IndexedDB как primary storage
2. Supabase для синхронизации
3. Автоматическая синхронизация при наличии сети
4. Очередь операций для офлайн-изменений

### Структура данных

```typescript
// lib/types/category.ts
export type Category = {
  id: string;
  user_id: string;
  name: string;
  type: "income" | "expense";
  icon: string | null;
  color: string | null;
  parent_id: string | null;
  sort_order: number;
  is_archived: boolean;
  created_at: string;
  updated_at: string;
};

export type CategoryWithChildren = Category & {
  children?: CategoryWithChildren[];
};
```

## 3. Референсы и вдохновение

### Популярные приложения для изучения

1. **YNAB (You Need A Budget)**
   - Чёткая иерархия категорий
   - Группы категорий (например, "Ежедневные расходы")
   - Цветовое кодирование
   - URL: https://www.youneedabudget.com

2. **Mint (Intuit)**
   - Автоматическая категоризация
   - Визуализация расходов по категориям
   - Группировка по типам

3. **PocketGuard**
   - Простой и понятный интерфейс
   - Минималистичный дизайн категорий

4. **Wallet (BudgetBakers)**
   - Иконки для каждой категории
   - Цветовое оформление
   - Подкатегории

### Рекомендации по дизайну

**Принципы:**

1. **Иерархия не глубже 2 уровней** (родитель → дочерняя)
   - Слишком глубокая иерархия усложняет навигацию
2. **Предустановленные категории при онбординге**
   - Новым пользователям предлагать базовый набор
   - Возможность кастомизации

3. **Быстрый доступ к часто используемым**
   - Недавно использованные категории вверху
   - Избранные категории

4. **Визуальная идентификация**
   - Иконка + цвет = быстрая идентификация
   - Консистентность в выборе цветов

5. **Поиск и фильтрация**
   - Поиск по названию
   - Фильтр по типу (доходы/расходы)
   - Фильтр по архивированным

## 4. План реализации

### Этап 1: Базовая функциональность (MVP)

- [ ] Тип `Category` в `lib/types/category.ts`
- [ ] Хук `useCategories` для работы с данными
- [ ] Компонент `CategoryList` — список категорий
- [ ] Компонент `CategoryForm` — форма создания/редактирования
- [ ] Страница `/dashboard/categories` с полным функционалом
- [ ] Переводы в `messages/en.json` и `messages/ru.json`

### Этап 2: Интеграция с транзакциями

- [ ] Компонент `CategorySelector` для выбора в транзакциях
- [ ] Интеграция в `TransactionForm`
- [ ] Отображение категории в списке транзакций

### Этап 3: Продвинутые функции

- [ ] Drag-and-drop для сортировки (десктоп)
- [ ] Архивация категорий
- [ ] Статистика по категориям
- [ ] Предустановленные наборы категорий

### Этап 4: Офлайн-поддержка (когда будет готов IndexedDB)

- [ ] Синхронизация категорий через `SyncManager`
- [ ] Локальное кеширование
- [ ] Офлайн-создание/редактирование

## 5. Компоненты для создания

```
components/features/categories/
├── CategoryList.tsx          # Список категорий (дерево/группы)
├── CategoryItem.tsx          # Элемент категории
├── CategoryForm.tsx          # Форма создания/редактирования
├── CategorySelector.tsx       # Выбор категории (для транзакций)
├── CategoryTree.tsx           # Древовидная структура
└── CategoryIconPicker.tsx    # Выбор иконки
```

## 6. Хуки для создания

```
lib/hooks/
└── useCategories.ts           # CRUD операции, кеширование
```

## 7. Переводы

Добавить в `messages/en.json` и `messages/ru.json`:

```json
{
  "categories": {
    "title": "Categories",
    "add": "Add Category",
    "edit": "Edit Category",
    "delete": "Delete Category",
    "archive": "Archive",
    "unarchive": "Unarchive",
    "income": "Income",
    "expense": "Expense",
    "name": "Name",
    "type": "Type",
    "parent": "Parent Category",
    "icon": "Icon",
    "color": "Color",
    "noParent": "No parent (top level)",
    "empty": "No categories yet",
    "emptyDescription": "Create your first category to start organizing your finances",
    "search": "Search categories",
    "filter": {
      "all": "All",
      "income": "Income only",
      "expense": "Expense only",
      "archived": "Archived"
    }
  }
}
```

## 8. Рекомендации по реализации

1. **Начать с простого**: плоский список без иерархии, затем добавить родительские категории
2. **Использовать существующие UI компоненты**: shadcn/ui (Card, Button, Input, Select)
3. **Следовать паттернам проекта**: как в BudgetForm, TransactionForm
4. **Типизация**: строгая типизация TypeScript
5. **Интернационализация**: все тексты через next-intl
6. **Адаптивность**: мобильная версия в приоритете
