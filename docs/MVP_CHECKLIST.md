# Чеклист MVP для Pennora

## Обзор

Этот документ содержит список задач, необходимых для выпуска минимально жизнеспособного продукта (MVP) приложения Pennora.

## 📊 Текущий статус MVP

**Прогресс: 85% завершено** ✅

### ✅ Выполнено (6/8 критичных задач):

- ✅ Переводы между счетами
- ✅ Редактирование и удаление транзакций
- ✅ Редактирование счетов
- ✅ Базовая статистика на дашборде
- ✅ PWA функциональность
- ✅ Обработка ошибок и валидация

### ⚠️ Осталось (2/8 критичных задач):

- ⚠️ Тестирование и багфиксы (~4-6 часов)
- ⚠️ Документация и деплой (~1-2 часа)

**Осталось для завершения MVP: ~5-8 часов работы**

## ✅ Уже реализовано

### Базовая инфраструктура

- ✅ Аутентификация (логин/регистрация через Supabase)
- ✅ Онбординг для новых пользователей
- ✅ База данных (Supabase с миграциями)
- ✅ Офлайн-режим (IndexedDB через Dexie)
- ✅ Синхронизация данных
- ✅ Мультиязычность (RU/EN)
- ✅ Темная/светлая тема
- ✅ Landing страница

### Функциональность

- ✅ Управление счетами (создание, просмотр, редактирование, удаление)
- ✅ Управление категориями (создание, редактирование, удаление, иерархия)
- ✅ Транзакции (создание доходов/расходов, просмотр, фильтрация по месяцам)
- ✅ Обработка чеков (загрузка изображений, PDF, EML файлов, фото с камеры)
  - OCR распознавание текста из чеков
  - Чтение QR-кодов ФНС
  - Автоматическое заполнение транзакций из чеков
  - Парсинг даты, суммы, описания, способа оплаты
- ✅ Дашборд с балансами счетов
- ✅ Мультивалютность (RUB, USD, EUR) с конвертацией
- ✅ Настройки профиля и валют

## ✅ Что уже реализовано для MVP

### Критичные функции

1. ✅ **Переводы между счетами** - полностью реализовано
2. ✅ **Редактирование и удаление транзакций** - полностью реализовано
3. ✅ **Редактирование счетов** - полностью реализовано

### Важные функции

4. ✅ **Базовая статистика на дашборде** - реализовано с сравнением с предыдущим месяцем
5. ✅ **PWA функциональность** - полностью реализовано
6. ✅ **Обработка ошибок и валидация** - полностью реализовано

## 📋 Детальный статус задач MVP

> **Примечание:** Большинство критичных и важных задач уже выполнено. Ниже приведен детальный статус каждой задачи.

### 1. Переводы между счетами (КРИТИЧНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Переводы между счетами работают с полной валидацией и отображением.

**Что было сделано:**

- [x] Добавлено поле выбора целевого счета (`to_account_id`) в `TransactionForm.tsx`
- [x] Поле показывается только когда тип транзакции = "transfer"
- [x] Реализована валидация: целевой счет должен отличаться от исходного
- [x] Обновлена схема валидации формы (добавлен `to_account_id` с условной валидацией через Zod `.refine()`)
- [x] Обновлена мутация создания транзакции для поддержки переводов
- [x] Обновлено отображение переводов в `TransactionList.tsx` (показывается "С [счет] → На [счет]")
- [x] Добавлены переводы для новых UI элементов в `messages/ru.json` и `messages/en.json`
- [x] Реализовано скрытие опции "transfer", если у пользователя меньше 2 счетов
- [x] Добавлена инвалидация кеша счетов при создании/обновлении/удалении переводов
- [x] Добавлена анимация появления/исчезновения поля целевого счета через framer-motion

**Файлы для изменения:**

- `components/features/transactions/TransactionForm.tsx`
- `components/features/transactions/TransactionList.tsx`
- `lib/query/mutations/transactions.ts`
- `messages/ru.json`
- `messages/en.json`

---

### 2. Редактирование и удаление транзакций (КРИТИЧНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Редактирование и удаление транзакций работают с полной валидацией и обработкой ошибок.

**Что было сделано:**

- [x] Добавлены кнопки "Редактировать" и "Удалить" в `TransactionList.tsx`
- [x] Реализовано удаление транзакции через мутацию `useDeleteTransaction`
- [x] Добавлен диалог подтверждения удаления с анимацией
- [x] Реализовано редактирование через `TransactionForm` с поддержкой `initialData`
- [x] Обновлена мутация обновления транзакции с оптимистичными обновлениями
- [x] Добавлены переводы для действий редактирования/удаления

**Файлы:**

- `components/features/transactions/TransactionList.tsx` - список с действиями
- `components/features/transactions/TransactionForm.tsx` - форма с поддержкой редактирования
- `lib/query/mutations/transactions.ts` - мутации для обновления и удаления
- `messages/ru.json` и `messages/en.json` - переводы для действий

---

### 3. Редактирование счетов (ВАЖНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Редактирование счетов работает для карт и наличных с полной валидацией.

**Что было сделано:**

- [x] Добавлена кнопка "Редактировать" в карточку счета на странице `/dashboard/accounts`
- [x] Создана форма редактирования счета (переиспользована логика из онбординга)
- [x] Реализовано обновление счета через Supabase с использованием мутации `useUpdateAccount`
- [x] Добавлены переводы для редактирования в `messages/ru.json` и `messages/en.json`
- [x] Реализованы отдельные формы для редактирования карт и наличных
- [x] Добавлена оптимистичная инвалидация кеша при обновлении счетов
- [x] Реализована обработка ошибок с переводами

**Файлы:**

- `app/(main)/dashboard/accounts/page.tsx` - страница с редактированием
- `lib/query/mutations/accounts.ts` - мутация обновления счета
- `messages/ru.json` и `messages/en.json` - переводы для редактирования

---

### 4. Базовая статистика на дашборде (ВАЖНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Дашборд показывает балансы счетов, статистику доходов/расходов за текущий месяц с сравнением с предыдущим месяцем.

**Что было сделано:**

- [x] Добавлены карточки с суммами доходов и расходов за текущий месяц
- [x] Добавлена карточка с балансом (доходы - расходы)
- [x] Реализовано сравнение с предыдущим месяцем (процентное изменение)
- [x] Используется React Query для загрузки статистики с кешированием
- [x] Добавлены переводы для статистики
- [x] Реализована конвертация валют для статистики

**Файлы:**

- `app/(main)/dashboard/page.tsx` - главная страница дашборда
- `app/(main)/dashboard/enhanced-statistics-cards.tsx` - компонент статистики
- `lib/query/queries/transactions.ts` - функция `fetchMonthlyStatistics`
- `messages/ru.json` и `messages/en.json` - переводы для статистики

---

### 5. PWA функциональность (ВЫПОЛНЕНО)

**Текущее состояние:** ✅ PWA полностью реализовано через официальный подход Next.js 16.

**Реализовано:**

- [x] Создан `app/manifest.ts` используя встроенную поддержку Next.js
- [x] Реализован Service Worker с Workbox CDN для кеширования
- [x] Настроено кеширование статических ресурсов, изображений и API запросов
- [x] Добавлены иконки приложения (192x192, 512x512, apple-touch-icon)
- [x] Обновлен `app/layout.tsx` с PWA метаданными
- [x] Реализована регистрация Service Worker
- [x] Интегрировано с существующей оффлайн-синхронизацией через IndexedDB

**Используемые технологии:**

- Workbox CDN (официальная библиотека Google)
- Встроенная поддержка манифеста Next.js 16
- Ручной Service Worker без webpack (работает с Turbopack)

**Файлы:**

- `app/manifest.ts` - манифест PWA
- `public/sw.js` - Service Worker с Workbox
- `app/components/service-worker-register.tsx` - регистрация SW
- `public/icons/` - иконки приложения

---

### 6. Обработка ошибок и валидация (ВАЖНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Обработка ошибок унифицирована, все формы используют переводы для валидации.

**Что было сделано:**

- [x] Добавлены информативные сообщения об ошибках через утилиту `lib/utils/errorHandler.ts`
- [x] Унифицирована валидация всех форм - все сообщения об ошибках используют переводы
- [x] Добавлена обработка сетевых ошибок с переводами для офлайн режима
- [x] Созданы компоненты `LoadingState` и `ErrorState` для единообразного отображения состояний
- [x] Обновлена обработка ошибок в мутациях React Query с поддержкой переводов
- [x] Добавлены переводы для всех сообщений об ошибках (RU/EN)
- [x] Улучшена обработка ошибок в формах через `setError` из React Hook Form

**Реализованные компоненты:**

- `lib/utils/errorHandler.ts` - утилита для обработки ошибок Supabase и преобразования в переведенные сообщения
- `components/ui/loading-state.tsx` - компонент состояния загрузки с анимациями
- `components/ui/error-state.tsx` - компонент состояния ошибки с кнопкой повтора
- Обновлены все мутации в `lib/query/mutations/` для использования переводов
- Обновлены формы (TransactionForm) для использования переводов в валидации

**Файлы:**

- `lib/utils/errorHandler.ts` - утилита обработки ошибок
- `components/ui/loading-state.tsx` - компонент загрузки
- `components/ui/error-state.tsx` - компонент ошибки
- `lib/query/mutations/transactions.ts` - мутации транзакций с переводами
- `lib/query/mutations/categories.ts` - мутации категорий с переводами
- `components/features/transactions/TransactionForm.tsx` - форма с переводами валидации
- `components/features/transactions/TransactionList.tsx` - список с обработкой ошибок
- `messages/ru.json` и `messages/en.json` - добавлены секции errors.validation, errors.mutations, errors.network

---

### 7. Тестирование и багфиксы (КРИТИЧНО)

**Что нужно сделать:**

- [ ] Протестировать весь пользовательский флоу:
  - Регистрация → Онбординг → Создание транзакций → Просмотр статистики
- [ ] Проверить работу офлайн-режима
- [ ] Проверить синхронизацию данных
- [ ] Проверить работу на мобильных устройствах
- [ ] Исправить найденные баги
- [ ] Проверить производительность (особенно при большом количестве транзакций)

---

### 8. Документация и деплой (ВАЖНО)

**Что нужно сделать:**

- [ ] Обновить README.md с инструкциями по деплою
- [ ] Настроить переменные окружения для production
- [ ] Подготовить инструкцию по настройке Supabase проекта
- [ ] Проверить, что все миграции применены
- [ ] Настроить CI/CD (если нужно)

---

### 9. Очистка кеша при сбросе данных ✅

**Текущее состояние:** ✅ Реализовано частично. Кеш очищается при использовании кнопки Reset, но не при обычном онбординге.

**Что было сделано:**

- [x] Реализована очистка кеша React Query в `reset-button.tsx` при сбросе данных
- [x] Очищаются кеши транзакций, статистики, счетов и категорий
- [x] Данные удаляются из базы данных через server action `resetAccounts`

**Что нужно улучшить:**

- [ ] Добавить очистку кеша при завершении обычного онбординга (не только через Reset)
- [ ] Убедиться, что IndexedDB персистентный кеш также очищается при сбросе

**Файлы:**

- `app/(main)/dashboard/reset-button.tsx` - очистка кеша при Reset
- `app/(main)/dashboard/actions.ts` - server action для сброса данных

---

## 🚫 Что НЕ нужно для MVP (можно отложить)

- ❌ Совместные бюджеты (семейные аккаунты) - сложная функция, не критична для MVP
- ❌ Детальная статистика и графики - можно добавить позже
- ❌ Экспорт данных - не критично для MVP
- ❌ Уведомления - можно добавить позже
- ❌ Мобильное приложение через Capacitor - сначала PWA

---

## Приоритеты

### Критично (должно быть в MVP):

1. ✅ Переводы между счетами - **ГОТОВО**
2. ✅ Редактирование и удаление транзакций - **ГОТОВО**
3. ⚠️ Тестирование и багфиксы - **В ПРОЦЕССЕ**

### Важно (желательно для MVP):

4. ✅ Редактирование счетов - **ГОТОВО**
5. ✅ Базовая статистика на дашборде - **ГОТОВО**
6. ✅ Обработка ошибок и валидация - **ГОТОВО**
7. ⚠️ Документация и деплой - **ТРЕБУЕТ ВНИМАНИЯ**

### Готово (можно отметить как выполненное):

8. ✅ PWA функциональность - **ГОТОВО**

---

## Оценка времени

### Выполнено ✅

- ✅ Переводы между счетами: ~2-3 часа - **ВЫПОЛНЕНО**
- ✅ Редактирование/удаление транзакций: ~2-3 часа - **ВЫПОЛНЕНО**
- ✅ Редактирование счетов: ~1-2 часа - **ВЫПОЛНЕНО**
- ✅ Базовая статистика: ~2-3 часа - **ВЫПОЛНЕНО**
- ✅ PWA: ~2-3 часа - **ВЫПОЛНЕНО**
- ✅ Обработка ошибок и валидация: ~2-3 часа - **ВЫПОЛНЕНО**

### Осталось ⚠️

- ⚠️ Тестирование и багфиксы: ~4-6 часов - **ТРЕБУЕТ ВНИМАНИЯ**
- ⚠️ Документация и деплой: ~1-2 часа - **ТРЕБУЕТ ВНИМАНИЯ**

**Прогресс MVP: ~85% завершено**

**Осталось для завершения MVP: ~5-8 часов работы**

---

## Следующие шаги

1. ✅ ~~Начать с критичных задач (переводы, редактирование транзакций)~~ - **ВЫПОЛНЕНО**
2. ✅ ~~Добавить важные функции (статистика, редактирование счетов)~~ - **ВЫПОЛНЕНО**
3. ✅ ~~Настроить PWA~~ - **ВЫПОЛНЕНО**
4. ⚠️ **Протестировать весь пользовательский флоу** - **ТЕКУЩАЯ ЗАДАЧА**
5. ⚠️ **Подготовить документацию для деплоя** - **ТЕКУЩАЯ ЗАДАЧА**
6. ⚠️ **Финальное тестирование и деплой** - **СЛЕДУЮЩИЙ ЭТАП**

## 📊 Статус MVP

**Общий прогресс: 85%**

### ✅ Готово (6/8 критичных задач):

- Переводы между счетами
- Редактирование и удаление транзакций
- Редактирование счетов
- Базовая статистика на дашборде
- PWA функциональность
- Обработка ошибок и валидация

### ⚠️ В процессе (2/8 критичных задач):

- Тестирование и багфиксы
- Документация и деплой

---

## 🔧 Задачи для улучшения и исправления

### Email подтверждение и SMTP

**Текущее состояние:** Подтверждение email отключено для упрощения разработки. Встроенный SMTP Supabase имеет ограничения (только для членов команды, лимит 3 письма/час).

**Что нужно сделать:**

- [ ] Настроить кастомный SMTP сервер для продакшена (Resend, SendGrid или AWS SES)
  - [ ] Создать аккаунт в выбранном SMTP провайдере
  - [ ] Настроить SMTP в Supabase Dashboard: `Settings > Auth > SMTP Settings`
  - [ ] Настроить DKIM, DMARC и SPF для домена (для лучшей доставки)
  - [ ] Протестировать отправку писем
- [ ] Включить подтверждение email после настройки SMTP
- [ ] Обновить email шаблоны в Supabase Dashboard для лучшей доставки
- [ ] Настроить кастомный домен для Auth (опционально, для лучшей репутации)

**Приоритет:** Средний (можно сделать после MVP, но важно для продакшена)

**Ресурсы:**

- [Документация Supabase по SMTP](https://supabase.com/docs/guides/auth/auth-smtp)
- [Resend для Supabase](https://resend.com/docs/send-with-supabase-smtp)
- [SendGrid для Supabase](https://www.twilio.com/docs/sendgrid/for-developers/sending-email/getting-started-smtp)

---

### Безопасность зависимостей

**Текущее состояние:** ✅ Частично исправлено. Использованы `pnpm.overrides` для обновления уязвимых зависимостей. Остается 1 moderate уязвимость в транзитивной зависимости без патча.

**Статус уязвимостей:**

- ✅ **form-data** (critical) - исправлено через `pnpm.overrides` (>=2.5.4)
- ✅ **tough-cookie** (moderate) - исправлено через `pnpm.overrides` (>=4.1.3)
- ⚠️ **request** (moderate) - SSRF уязвимость, патча нет (пакет не поддерживается)

**Цепочка зависимостей:**

```
eml-parser → html-pdf → phantomjs-prebuilt → request → form-data/tough-cookie
```

**Что было сделано:**

- [x] Добавлены `pnpm.overrides` в `package.json` для обновления `form-data` и `tough-cookie`
- [x] Проведен аудит зависимостей через `pnpm audit`
- [x] Уязвимости снижены с 3 (1 critical + 2 moderate) до 1 (moderate)

**Что нужно сделать:**

- [ ] Мониторить обновления `eml-parser` на предмет исправления уязвимостей
- [ ] Рассмотреть альтернативы `eml-parser` для парсинга EML файлов:
  - [ ] Исследовать альтернативные библиотеки (mailparser, node-imap и др.)
  - [ ] Оценить возможность написания собственного парсера EML
  - [ ] Проверить, можно ли изолировать использование `eml-parser` в отдельном сервисе
- [ ] Добавить дополнительные проверки безопасности для EML файлов:
  - [ ] Ограничение размера файла
  - [ ] Валидация структуры EML файла
  - [ ] Санитизация содержимого перед обработкой
- [ ] Настроить автоматический мониторинг уязвимостей:
  - [ ] Включить Dependabot alerts в GitHub
  - [ ] Настроить автоматические обновления для безопасных патчей
  - [ ] Добавить проверку уязвимостей в CI/CD pipeline

**Оценка риска:**

- **Текущий риск:** Низкий
  - `eml-parser` используется только на сервере (API route)
  - Обрабатываются только EML файлы от авторизованных пользователей
  - Есть базовая валидация типа файла
  - Уязвимость в `request` связана с SSRF через redirect, что маловероятно для EML файлов

**Приоритет:** Средний (можно отложить после MVP, но важно для долгосрочной безопасности)

**Ресурсы:**

- [GitHub Security Advisories](https://github.com/advisories)
- [npm Security Best Practices](https://docs.npmjs.com/security-best-practices)
- [pnpm Overrides Documentation](https://pnpm.io/package_json#pnpmoverrides)

---
