# Чеклист MVP для Pennora

## Обзор

Этот документ содержит список задач, необходимых для выпуска минимально жизнеспособного продукта (MVP) приложения Pennora.

## ✅ Уже реализовано

### Базовая инфраструктура

- ✅ Аутентификация (логин/регистрация через Supabase)
- ✅ Онбординг для новых пользователей
- ✅ База данных (Supabase с миграциями)
- ✅ Офлайн-режим (IndexedDB через Dexie)
- ✅ Синхронизация данных
- ✅ Мультиязычность (RU/EN)
- ✅ Темная/светлая тема
- ✅ Landing страница

### Функциональность

- ✅ Управление счетами (создание, просмотр, удаление)
- ✅ Управление категориями (создание, редактирование, удаление, иерархия)
- ✅ Транзакции (создание доходов/расходов, просмотр, фильтрация по месяцам)
- ✅ Дашборд с балансами счетов
- ✅ Мультивалютность (RUB, USD, EUR) с конвертацией
- ✅ Настройки профиля и валют

## ❌ Что нужно для MVP

### 1. Переводы между счетами (КРИТИЧНО) ✅

**Текущее состояние:** ✅ Реализовано полностью. Переводы между счетами работают с полной валидацией и отображением.

**Что было сделано:**

- [x] Добавлено поле выбора целевого счета (`to_account_id`) в `TransactionForm.tsx`
- [x] Поле показывается только когда тип транзакции = "transfer"
- [x] Реализована валидация: целевой счет должен отличаться от исходного
- [x] Обновлена схема валидации формы (добавлен `to_account_id` с условной валидацией через Zod `.refine()`)
- [x] Обновлена мутация создания транзакции для поддержки переводов
- [x] Обновлено отображение переводов в `TransactionList.tsx` (показывается "С [счет] → На [счет]")
- [x] Добавлены переводы для новых UI элементов в `messages/ru.json` и `messages/en.json`
- [x] Реализовано скрытие опции "transfer", если у пользователя меньше 2 счетов
- [x] Добавлена инвалидация кеша счетов при создании/обновлении/удалении переводов
- [x] Добавлена анимация появления/исчезновения поля целевого счета через framer-motion

**Файлы для изменения:**

- `components/features/transactions/TransactionForm.tsx`
- `components/features/transactions/TransactionList.tsx`
- `lib/query/mutations/transactions.ts`
- `messages/ru.json`
- `messages/en.json`

---

### 2. Редактирование и удаление транзакций (КРИТИЧНО)

**Текущее состояние:** Можно создавать транзакции, но нет возможности редактировать или удалять.

**Что нужно сделать:**

- [ ] Добавить кнопки "Редактировать" и "Удалить" в `TransactionList.tsx`
- [ ] Реализовать удаление транзакции (уже есть функция `deleteTransaction` в хуке?)
- [ ] Добавить диалог подтверждения удаления
- [ ] Поддержать редактирование через `TransactionForm` (передавать `initialData`)
- [ ] Обновить мутацию обновления транзакции (если нужно)
- [ ] Добавить переводы для действий редактирования/удаления

**Файлы для изменения:**

- `components/features/transactions/TransactionList.tsx`
- `components/features/transactions/TransactionForm.tsx`
- `lib/hooks/useTransactions.ts` (проверить наличие методов)
- `lib/query/mutations/transactions.ts`
- `messages/ru.json`
- `messages/en.json`

---

### 3. Редактирование счетов (ВАЖНО)

**Текущее состояние:** Можно создавать и удалять счета, но нет редактирования.

**Что нужно сделать:**

- [ ] Добавить кнопку "Редактировать" в карточку счета на странице `/dashboard/accounts`
- [ ] Создать форму редактирования счета (можно переиспользовать логику из онбординга)
- [ ] Реализовать обновление счета через Supabase
- [ ] Добавить переводы для редактирования

**Файлы для изменения:**

- `app/(main)/dashboard/accounts/page.tsx`
- `messages/ru.json`
- `messages/en.json`

---

### 4. Базовая статистика на дашборде (ВАЖНО)

**Текущее состояние:** Дашборд показывает только балансы счетов.

**Что нужно сделать:**

- [ ] Добавить карточки с суммами доходов и расходов за текущий месяц
- [ ] Добавить карточку с балансом (доходы - расходы)
- [ ] Использовать React Query для загрузки статистики
- [ ] Добавить переводы для статистики

**Файлы для изменения:**

- `app/(main)/dashboard/page.tsx`
- `app/(main)/dashboard/balance-cards.tsx` (расширить или создать новый компонент)
- `lib/query/queries/transactions.ts` (добавить функцию для статистики)
- `messages/ru.json`
- `messages/en.json`

---

### 5. PWA функциональность (ЖЕЛАТЕЛЬНО)

**Текущее состояние:** Service Worker - заглушка, манифест есть, но не настроен.

**Что нужно сделать:**

- [ ] Настроить `next-pwa` или реализовать Service Worker вручную
- [ ] Настроить кеширование статических ресурсов
- [ ] Добавить иконки приложения (проверить наличие в `public/icons/`)
- [ ] Протестировать установку PWA на мобильных устройствах
- [ ] Обновить `next.config.ts` для поддержки PWA

**Файлы для изменения:**

- `next.config.ts`
- `public/sw.js` (или удалить, если используется next-pwa)
- `public/manifest.json` (проверить и обновить)
- `package.json` (добавить next-pwa, если нужно)

---

### 6. Обработка ошибок и валидация (ВАЖНО)

**Текущее состояние:** Есть базовая обработка ошибок, но можно улучшить.

**Что нужно сделать:**

- [ ] Добавить более информативные сообщения об ошибках
- [ ] Проверить валидацию всех форм
- [ ] Добавить обработку сетевых ошибок (офлайн режим)
- [ ] Добавить индикаторы загрузки везде, где нужно
- [ ] Проверить обработку ошибок в мутациях React Query

**Файлы для проверки:**

- Все формы (TransactionForm, CategoryForm, AccountForm)
- Все мутации в `lib/query/mutations/`
- Компоненты с загрузкой данных

---

### 7. Тестирование и багфиксы (КРИТИЧНО)

**Что нужно сделать:**

- [ ] Протестировать весь пользовательский флоу:
  - Регистрация → Онбординг → Создание транзакций → Просмотр статистики
- [ ] Проверить работу офлайн-режима
- [ ] Проверить синхронизацию данных
- [ ] Проверить работу на мобильных устройствах
- [ ] Исправить найденные баги
- [ ] Проверить производительность (особенно при большом количестве транзакций)

---

### 8. Документация и деплой (ВАЖНО)

**Что нужно сделать:**

- [ ] Обновить README.md с инструкциями по деплою
- [ ] Настроить переменные окружения для production
- [ ] Подготовить инструкцию по настройке Supabase проекта
- [ ] Проверить, что все миграции применены
- [ ] Настроить CI/CD (если нужно)

---

## 🚫 Что НЕ нужно для MVP (можно отложить)

- ❌ Совместные бюджеты (семейные аккаунты) - сложная функция, не критична для MVP
- ❌ Детальная статистика и графики - можно добавить позже
- ❌ Экспорт данных - не критично для MVP
- ❌ Уведомления - можно добавить позже
- ❌ Мобильное приложение через Capacitor - сначала PWA

---

## Приоритеты

### Критично (должно быть в MVP):

1. Переводы между счетами
2. Редактирование и удаление транзакций
3. Тестирование и багфиксы

### Важно (желательно для MVP):

4. Редактирование счетов
5. Базовая статистика на дашборде
6. Обработка ошибок и валидация
7. Документация и деплой

### Желательно (можно добавить позже):

8. PWA функциональность

---

## Оценка времени

- Переводы между счетами: ~2-3 часа
- Редактирование/удаление транзакций: ~2-3 часа
- Редактирование счетов: ~1-2 часа
- Базовая статистика: ~2-3 часа
- PWA: ~2-3 часа
- Тестирование и багфиксы: ~4-6 часов
- Документация: ~1-2 часа

**Итого для MVP: ~14-22 часа работы**

---

## Следующие шаги

1. Начать с критичных задач (переводы, редактирование транзакций)
2. Протестировать основной флоу
3. Добавить важные функции (статистика, редактирование счетов)
4. Настроить PWA (если останется время)
5. Финальное тестирование и деплой
