# Планирование будущих функций

**Дата создания:** 2025-12-30  
**Версия проекта:** 1.3.2

---

## 📋 Содержание

1. [Поиск по транзакциям](#поиск-по-транзакциям)
2. [Debounce для фильтров и поиска](#debounce-для-фильтров-и-поиска)

---

## Поиск по транзакциям

### Описание

Добавление функционала текстового поиска по транзакциям для быстрого нахождения нужных записей.

### Требования

- Текстовое поле поиска в интерфейсе транзакций
- Поиск по описанию транзакции
- Поиск по названию категории
- Поиск по сумме (опционально)
- Фильтрация результатов в реальном времени
- Подсветка найденных совпадений (опционально)

### Технические детали

**Файлы для изменения:**

- `components/features/transactions/TransactionFilters.tsx` (реализовать компонент)
- `components/features/transactions/TransactionPageContent.tsx` (добавить поле поиска)
- `lib/query/queries/transactions.ts` (добавить параметр поиска в `fetchTransactions`)
- `lib/query/keys.ts` (обновить query keys для включения поискового запроса)

**Оценка времени:** 4-6 часов

### Примечания

- Поиск должен работать с существующей системой фильтров (месяц/год)
- Использовать debounce для оптимизации запросов (см. ниже)
- Рассмотреть возможность клиентской фильтрации для быстрого отклика

---

## Debounce для фильтров и поиска

### Описание

Оптимизация запросов к базе данных через debounce для текстового поиска и других фильтров, которые могут часто изменяться.

### Проблема

Текстовый поиск и некоторые фильтры могут вызывать запросы при каждом изменении без задержки, что приводит к:

- Избыточным запросам к БД
- Плохому UX (множественные перезагрузки)
- Нагрузке на сервер

### Решение

- Создать хук `useDebounce` или использовать готовую библиотеку (например, `use-debounce`)
- Применить debounce к текстовому поиску (300-500ms задержка)
- Применить debounce к другим фильтрам, если они будут добавлены в будущем
- Оптимизировать количество запросов к БД

### Технические детали

**Файлы для изменения:**

- `lib/hooks/useDebounce.ts` (создать хук)
- `components/features/transactions/TransactionFilters.tsx` (применить debounce к поиску)
- `components/features/transactions/TransactionPageContent.tsx` (интегрировать debounced поиск)

**Оценка времени:** 2-3 часа

### Примечания

- **Важно:** Debounce нужен только для текстового поиска, не для `MonthYearSelector` (Select компоненты не требуют debounce)
- TanStack Query уже кеширует результаты, но debounce уменьшит количество запросов при вводе
- Рекомендуемая задержка: 300-500ms для поиска

### Зависимости

- Реализация debounce зависит от добавления поиска по транзакциям
- Рекомендуется реализовать вместе с поиском

---

## Приоритизация

### Высокий приоритет

1. Поиск по транзакциям — улучшит UX и будет востребован пользователями

### Средний приоритет

2. Debounce для поиска — оптимизация, которая должна быть реализована вместе с поиском

---

## Связанные задачи

- См. также: [`roadmap.md`](./roadmap.md) для других задач оптимизации
- Компонент `TransactionFilters.tsx` уже существует как заглушка и готов к реализации

---

**Последнее обновление:** 2025-12-30
